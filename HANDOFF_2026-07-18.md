# Crossed Out — Session Handoff (Jul 18, 2026)

Snapshot of everything done this session + exactly what to deploy/build next.
Companion docs: MASTER_PLAN.md, DESIGN_LANGUAGE.md, GAP_ANALYSIS_AND_ROADMAP.md
(§7 G19 spec, §8 backlog, §9 deterministic-first principle), the RUNBOOK files.

## 1. Bible verse tagging — DONE
- Full BSB corpus tagged + strict-reviewed. Final: **4,158 approved / 4,105
  rejected** (gpt-5.2 reviewer, `REVIEW_MODEL` in `scripts/.env.run`). All **8/8
  targeted flip checks** correct; two independent Sonnet audits agreed 79–83%,
  with disagreements only on low-harm "loose approves," reject side clean.
- Tightened **pre-screen** (`scripts/prescreen_approved.py`) run on the
  high-volume/low-rejection categories. Tyler spot-checked ~25 flags (all good)
  and is trusting the pass. **Backlog:** full hand-review of the flagged CSV when
  fresh (GAP §8).
- Tooling in `scripts/`: `tag_bible.py`, `review_tags.py`, `verify_review.py`,
  `prescreen_approved.py`, `run.sh`. All run from **your Mac terminal** (has
  network + creds); the cloud session cannot reach the DB.

## 2. G19 Devotionals — slice 1 (foundation) — BUILT, needs apply+build
Migration `0016_devotionals.sql` + `CrossedOut/Features/Devotionals/*`,
`SupabaseService+Devotionals.swift`, Models + MoreHubView edits.
- Tables: `devotionals` (built-in catalog), `user_devotionals` (independent
  study), `devotional_feedback` (helpful signal on both). RPCs
  `submit_devotional_feedback`, `today_devotional`. 3 seed devotionals.
- App: **More → Devotionals** hub; built-in detail; independent-study composer;
  "was this helpful?" control on both surfaces.
- See `G19_DEVOTIONALS_RUNBOOK.md`.

## 3. G19 Tier 2 + Tier 3 (deterministic re-roll + gated AI) — BUILT, needs apply+build+deploy
Principle GAP §9: deterministic by default, AI only on explicit tap.
- **Migration `0017_devotional_reroll_and_ai.sql`:** `next_devotional(exclude)`
  (Tier 2 devotional re-roll), `reroll_prepare_today_verse()` (Tier 2 daily-verse
  re-roll — penalizes + clears today's pick so the existing engine re-picks; no
  scoring duplication), and `devotional_ai_usage` + `increment_devotional_ai_usage`
  (Tier 3 per-user daily cap, mirrors kyra_usage).
- **Edge function `devotional_suggest`** (Tier 3): JWT + anon-check + cap →
  embeds the user's context → retrieves a **real** verse via `match_verses_text`
  → frames a short reflection grounded strictly in that verse (select-and-frame,
  not free-generation). Deploy: `./supabase/deploy_devotional_suggest.sh`.
- App: "Show me another verse" on the Today card (`AppState.rerollTodayVerse`);
  "Show me another" + "Ask AI for one" on the Devotionals hub; `AiSuggestionSheet`
  (describe context → grounded suggestion → save to studies).

## 4. Deploy / build checklist (run on your Mac)
1. **Apply migrations** in the Supabase SQL editor (they have functions + RLS):
   ```
   cd ~/Projects/Crossed*/ && pbcopy < supabase/migrations/0016_devotionals.sql   # paste → Run
   pbcopy < supabase/migrations/0017_devotional_reroll_and_ai.sql                 # paste → Run
   ```
2. **Deploy the Tier 3 edge function** (needs `supabase login`):
   ```
   ./supabase/deploy_devotional_suggest.sh
   # optional caps: supabase secrets set DEVO_DAILY_LIMIT=5 DEVO_MODEL=gpt-4o-mini --project-ref wqumwxoiqsiwizlftojq
   ```
3. **Regenerate + build:** `xcodegen generate` → build `CrossedOut.xcodeproj` in Xcode.
4. **Smoke test:** More → Devotionals (today card, Show-me-another, Ask-AI → save);
   Today tab verse card → "Show me another verse" re-rolls; helpful controls persist.

## 5. Open decisions (for the next G19 slice)
- **Built-in catalog authoring:** the seed is 3 devotionals — curated vs.
  AI-assisted? And a real **style taxonomy** (`devotionals.style`).
- **Preference recommender (Tier 4-ish):** turn `devotional_feedback` into per-user
  style/topic affinities; make `today_devotional()` / suggestions preference-aware.
- **Tier 3 model + cap:** `DEVO_MODEL` default `gpt-4o-mini`, `DEVO_DAILY_LIMIT` 5.
  Confirm, and decide whether AI suggestions are a **Plus** perk.
- Promote today's devotional onto the **Today tab** (currently under More).

## 6. Still-open LAUNCH BLOCKERS (unchanged this session — do before submit)
- **Phase 1 hardening is coded but NOT deployed:** disable anonymous sign-ins +
  enable Apple provider in the Supabase dashboard; apply migrations `0007`/`0008`;
  deploy the hardened Kyra function; smoke test. Full steps in `NEXT_PHASES.md`.
- **EULA / "no objectionable content" acceptance** at the auth step (App Review
  Guideline 1.2 UGC — likely rejection without it). Not yet built.
- **Monitoring** (TelemetryDeck + Sentry) — recommended pre-launch.

## 7. Housekeeping
- **Unpushed/uncommitted:** the 14 prior commits are pushed; everything from this
  session (tagging tooling, G19 slice 1, Tier 2/3, all docs) is uncommitted.
  Run: `git add -A && git commit -m "..." && git push` (note: `commit`/`push`
  need the `git` prefix — that's what failed earlier).
- **Rotate secrets:** the DB password and OpenAI key passed through the cloud
  session and sit in `scripts/.env.run` / `supabase/.env.local`. Rotate both.
- **Verification caveat:** the cloud session can't compile Swift or reach the DB,
  so the Xcode build + your smoke tests are the verification step for all G19 code.
```
```
