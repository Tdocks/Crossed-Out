# Crossed Out — Session Handoff
_Last updated: Jul 16, 2026. Read fully before doing anything._

## LATEST (Jul 16) — Phase 1 launch hardening done in code (commit b3c7e1f), NOT deployed
Anonymous auth removed app-wide; real Apple/email account now REQUIRED (mandatory
auth step in onboarding + full-screen gate for signed-out users). Kyra edge function
now verifies the user JWT, rejects anon/unauthenticated (401/403), and enforces a
per-user daily cap (KYRA_DAILY_LIMIT default 30 -> 429). Migrations 0007_require_auth
+ 0008_kyra_usage added but NOT applied. Build green. **App Store submission is on
hold (Tyler's call).** See **NEXT_PHASES.md** for the full runbook (git push, disable
anon + enable Apple in Supabase dashboard, apply migrations, deploy kyra, rotate DB
password + OpenAI key) and the Phase 2–5 plan. Two research reports (ElevenLabs audio
pricing/plan; church live-stream integration + directory) delivered in chat.

## STRATEGIC (Jul 16) — read GAP_ANALYSIS_AND_ROADMAP.md
Honest audit of the shipped app vs the FULL master plan (the "Christian life OS"
vision) + a 4-stream competitive/blindspot study. Bottom line: the build is a
polished shell of ~Phase 1 (~20-25% of planned scope), and several finished-looking
screens are actually MOCK/STUB (Explore = 100% fake; Attend "Watch Live" = fake;
Bridge = stub) — do not ship them as-is. Two real bugs flagged: church accent-color
mismatch, and onboarding persisting hardcoded name "Tyler" for every user. The doc
has a full gap register (G1-G20) and a P0/P1/P2 phased roadmap. Six product decisions
are pending at the end of that doc before executing P0.

## What this project is
Premium Christian iOS app ("Living Manuscript" aesthetic — printed Bible × Kinfolk).
SwiftUI, iOS 17, Xcode project generated by **xcodegen** (`project.yml`), Supabase
backend, Kyra AI chat via a Supabase Edge Function proxying OpenAI (model
`gpt-5.6-luna`). Full product plan: see the project instructions/plan doc in chat;
design language: warm paper light mode / candlelight dark mode, Playfair Display
(scripture/display) + Inter (UI), Color.co* adaptive tokens, custom monoline icons.

## Critical workflow quirks (will bite you)
1. **The folder name ends with a SPACE**: `/Users/tylerdockswell/Projects/Crossed Out /`.
   Cowork's built-in Read/Write/Edit tools are BLOCKED on this path. Use Desktop
   Commander MCP tools for all file work (read_file / edit_block / write_file in
   ≤30-line chunks) and start_process for shell. This is the established, authorized
   workflow — when delegating to subagents, describe it that way; do NOT phrase it
   as "built-in tools are blocked, bypass with Desktop Commander" (that framing has
   caused subagents to refuse as suspected injection).
2. Build loop: `cd "/Users/tylerdockswell/Projects/Crossed Out " && /opt/homebrew/bin/xcodegen generate`
   (only needed when files are added/removed) then
   `xcodebuild -project CrossedOut.xcodeproj -scheme CrossedOut -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -derivedDataPath build build`.
3. Visual verification: env var `CO_SCREEN=<today|bible|community|attend|more|kyra|progress|bridge|explore|churches|give|settings|onboarding>`
   jumps straight to a screen. Pattern:
   `SIMCTL_CHILD_CO_SCREEN=today xcrun simctl launch --terminate-running-process "iPhone 17 Pro" com.tdocks.crossedout`
   then `xcrun simctl io "iPhone 17 Pro" screenshot path.png`. View screenshots by
   copying into the session outputs folder via the sandbox mount.
4. Tyler prefers Opus/Sonnet **subagents** for build work to cut token cost; keep the
   orchestrator for integration, builds, browser work, verification.

## Backend (Supabase — Tyler's own project, NOT the MCP-connected account)
- Project: `wqumwxoiqsiwizlftojq` (https://wqumwxoiqsiwizlftojq.supabase.co)
- Publishable key: in `CrossedOut/Services/SupabaseService.swift` (SupabaseConfig)
- DB access ONLY via session pooler (direct host is IPv6-only):
  `psql "host=aws-0-us-east-1.pooler.supabase.com port=5432 dbname=postgres user=postgres.wqumwxoiqsiwizlftojq sslmode=require"`
  Password: ask Tyler (he plans to rotate it; it is not stored in the repo).
- Migrations: `supabase/migrations/0001–0006` (schema, seeds, full Bible, RPCs,
  study/engagement, safety/account). 93k verses in `bible_verses` (BSB/WEB/KJV,
  public domain — NIV/ESV deferred until post-launch licensing; requires an LLC).
- Edge function `kyra`: `supabase/functions/kyra/index.ts`, deployed & live.
  Deploy: `./supabase/deploy_kyra.sh` (uses `--use-api`, no Docker; OpenAI key in
  git-ignored `supabase/.env.local`; Tyler plans to rotate that key too).
- Anonymous sign-ins ENABLED. Sign in with Apple button is built, but Tyler must
  still enable the Apple provider in Supabase dashboard (Auth → Providers → Apple,
  authorized client ID `com.tdocks.crossedout`) — until then it errors gracefully.

## Apple (account owner: Tyler Dockswell, individual)
- Team ID `Q9ZH8AA9NY`. Bundle `com.tdocks.crossedout` (SIWA + Push enabled).
- ASC app: "Crossed Out: Daily Devotions", Apple ID **6790935043**, subtitle
  "Scripture for Real Life", category Lifestyle, SKU crossedout-ios-001.
- API keys in `~/.appstoreconnect/private_keys/`: **AV4QQKM7Q5 (Admin — use this;
  App Manager key 8QAQM65DBD cannot cloud-sign)**. Issuer `ac46a433-b795-401f-8044-832650880809`.
  (A stray AuthKey_LA7D5539T3.p8 in ~/Downloads — Tyler to verify/revoke.)
- Upload flow (proven): archive with `-destination 'generic/platform=iOS'` +
  `-allowProvisioningUpdates` + auth-key flags, then `-exportArchive` with
  `ExportOptions.plist` (app-store-connect / upload / automatic / manageAppVersionAndBuildNumber).
- TestFlight: build 1.0(1) and 1.0(2) uploaded. Build 2 has all device-feedback fixes.
- Privacy policy live: https://tdocks.github.io/Crossed-Out/privacy.html (GitHub
  Pages from `docs/`, repo `Tdocks/Crossed-Out`, gh CLI authenticated).

## App state (all pushed to GitHub main)
Done: full design system + 13 screens light/dark; onboarding (art illustration,
brand wordmark matching reference, flow-layout chips, 24 focus categories); Today
(profile-driven "You said"/focus, verse 22pt, premium speech voice + audio session,
check-in → re-recommendation → streak, pray sheet, devotional cross-off, bell→Settings);
Bible reader (full 66-book/chapter/translation navigation from Supabase, context-menu
highlight/note/bookmark/share, chapter paging, notes list); Kyra (live GPT via edge
function, verse context chip, FocusState keyboard fix); Community (4 working tabs,
composer, pray/encourage RPCs, report/block); Journey (streak, grace days, working
items sync, real rhythm from daily_completions); Attend (service detail, save church,
plan-a-visit sheet, see-all); Church Finder (filter menu, save hearts); Give
(LINK-OUT ONLY — Tyler is not a nonprofit, never collect donations in-app); Settings
(profile edit, translation, appearance, daily reminder notifications, delete account);
email auth with anonymous linking + SIWA button; empty states; offline banner;
Dynamic Type; app icon (light/dark/tinted).

## IN FLIGHT — App Store Connect metadata (browser via Claude-in-Chrome, Tyler logged in)
Completed & saved on the v1.0 version page: 6 screenshots (6.5" 1284×2778, made from
`appstore/store_ss_*.png`), promotional text, full description, keywords
(`bible,devotional,prayer,scripture,christian,faith,jesus,verse,daily,church,worship,study`),
support URL, copyright "2026 Tyler Dockswell".
**Stopped mid-flow on the App Privacy page**: clicked Edit on Privacy Policy URL and
focused the field but did NOT type/save. Resume there:
1. Enter `https://tdocks.github.io/Crossed-Out/privacy.html` → Save.
2. "Get Started" questionnaire. Truthful answers: collects Contact Info→Email address
   (app functionality, linked to identity); User Content→Other user content (prayers,
   notes, posts, Kyra messages; app functionality, linked); Identifiers→User ID
   (app functionality, linked). No tracking, no ads, nothing used for tracking.
   Then **Publish**.
3. Age rating questionnaire (App Information page): all violence/sex/gambling = None;
   app HAS unmoderated-ish UGC but with report/block + no DMs — answer the
   social/UGC questions truthfully (community features: yes; user-generated content:
   yes). Expect 4+ or 12+ outcome.
4. Select build 1.0(2) on the version page once processed, complete App Review
   contact info (Tyler: tdoxwell@icloud.com, phone 17726268991), Pricing (Free,
   all territories), then Add for Review — CONFIRM WITH TYLER before submitting.

## Remaining backlog (post-submission)
- Kyra streaming responses + rate limiting/credits (free-tier cost controls per plan).
- Pre-recorded/ElevenLabs audio for Listen if Tyler still unhappy with premium voice.
- Attend live streaming player (real video), church claiming/verification (Phase 3).
- Crossed Out Plus via StoreKit IAP; widgets; push via APNs; journeys content engine.
- Rotate: DB password + OpenAI key (both passed through chat).

## Where things live
- Repo root = the trailing-space folder. `CrossedOut/` app source, `supabase/`,
  `docs/` (Pages site), `appstore/` (store screenshots), `screenshots/` (dev),
  `brand/` (icon masters), `ExportOptions.plist`, `project.yml`.
- Memory files in the Cowork session also track this (crossed-out-project).
